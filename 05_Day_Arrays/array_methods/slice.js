// Cortar vários itens no intervalo. Leva dois parâmetros (inicial e final)

const numbers = [1, 2, 3, 4, 5];

console.log(numbers.slice());
console.log(numbers.slice(0));
console.log(numbers.slice(0, numbers.length));
console.log(numbers.slice(2, 4)); // Remove a posição 0 e 1


