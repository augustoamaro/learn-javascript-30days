// O operador ternário permite escrever uma condição. 

let isRaining = true;

isRaining
    ? console.log('Você precisa de uma capa de chuva.')
    : console.log('Não há necessidade de capa de chuva.')
// O operador ternário permite escrever uma condição. 

isRaining = false;

isRaining
    ? console.log('Você precisa de uma capa de chuva.')
    : console.log('Não há necessidade de capa de chuva.')

    let number = 5
        number > 0
            ? console.log(`O ${number} é um número positivo`)
            : console.log(`O ${number} é um número negativo`)
        number = - 5

        number > 0
            ? console.log(`O ${number} é um número positivo`)
            : console.log(`O ${number} é um número negativo`)
    
