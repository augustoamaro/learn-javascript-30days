// Para definir uma classe, precisamos da palavra-chave class, o nome de uma classe em CamelCase e o código de bloco (duas chaves).

class Person {
   // o código vai aqui
}


// Instanciação de classe - Significa criar um objeto de uma classe. Precisamos da palavra-chave new e chamamos o nome da classe após a palavra new

class Pessoa {
    // o código vai aqui
}

const pessoa = new Pessoa()
console.log(pessoa);

// Criado um objeto pessoa. Como a classe ainda não tinha nenhuma propriedade, o objeto também está vazio.
// Vamos usar o construtor de classe para passar propriedades diferentes para a classe.



