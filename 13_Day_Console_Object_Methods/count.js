// Imprime o número de vezes que este console.count() é chamado. Leva uma parâmetro de rótulo de string. É muito útil contar o número de vezes que uma função é chamada.

const func = () => {
    console.count('Function has been called')
  }
  func()
  func()
  func()